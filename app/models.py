# Handles anything to do with the database
# from flask import current_app as app

# # base class for model is db.model and stored on the SQL instance created

# # Declare a Post Model with three attributes 
# class Post(db.model):

#     id = db.Column(db.Integer(), primary_key=True)
#     name = db.Column(db.String(80))
#     # post_text = db.Column(dp.String(255), unique=True)

#     def __init__(self, name):
#         self.name = name
        

